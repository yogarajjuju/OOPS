package interfaces.extendes2;
// We can extends interfaces in java it used for interfaces to interfaces
// to link a interface to other interface we use extends keyword

import interfaces.extendes.A;

public interface B extends A {
    void greet();
}
